MEMORY {
  ZP:      file = "", define = yes, start = $0002, size = $001A, type = rw;
  MIDROM:  file = %O, define = yes, start = $8000, size = $4000, type = ro;
  HIGHROM: file = %O, define = yes, start = $C000, size = $4000, type = ro;
}
SEGMENTS {
  CODE:     load = MIDROM, type = ro, align = $100;
  ZEROPAGE: load = ZP,     type = zp;
}
